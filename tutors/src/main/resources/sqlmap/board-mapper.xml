<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board.mapper">

	<select id="view" resultType="board.BoardVO">
		select b.*, (select name from member
					where member.id=b.board_id) name
		from board b
		where board_num = #{board_num, jdbcType=VARCHAR}
	</select>
	
	<insert id="insert">
		insert into board(board_num, board_id, board_content, board_nickname, board_image_path, board_image_name)
		values (seq_board.nextval, #{board_id}, #{board_content}, #{board_nickname, jdbcType=VARCHAR}, #{board_image_path, jdbcType=VARCHAR}, #{board_image_name, jdbcType=VARCHAR})
	</insert>
	
	<select id="totalList" resultType="integer">
		select count(*) from board where board_content like '%'|| #{keyword, jdbcType=VARCHAR} ||'%'
	</select>
	
	
	<select id="list" resultType="board.BoardVO">
		select rownum no, n.*, (select name from member m
								where m.id=n.board_id) name
		from (select * from board) n
		order by  board_notice asc, no desc
	</select>

</mapper>